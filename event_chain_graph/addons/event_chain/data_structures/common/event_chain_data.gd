tool
class_name EventChainData

# 基础图形节点

var unique_id := "event_chain_data"
var display_name := "EventChainData"
var category := "No category"
var description := "base description"

var ReferenceLineArray := []

func add_reference_line(node):
	ReferenceLineArray.append(ReferenceLineArray)
	
func remove_reference_line(node):
	ReferenceLineArray.erase(node)
